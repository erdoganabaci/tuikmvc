@model IEnumerable<Category>

    <div class="list-group">
        @foreach (var i in Model)
        {
            @*Category ismi*@
        <a  asp-controller="Home" asp-action="Index" asp-route-id="@i.Id" class="list-group-item list-group-item-action list-group-item-success @(ViewBag.SelectedCategory==i.Id.ToString()?"active":"")" >@i.Name</a>
        
        }
    <a href="  "></a>

    </div>